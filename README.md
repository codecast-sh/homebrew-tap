# Codecast Homebrew Tap

```sh
brew install codecast-sh/tap/codecast
```

Installs the `codecast` and `cast` commands. The formula is updated automatically on every CLI release.

Other install channels: https://codecast.sh/download
